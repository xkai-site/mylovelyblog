# My Lovely Blog 阅读体验与内容管理改造计划

> 状态：代码与本地检查已完成，等待用户 push 后的工作流构建和线上验收。按用户确认，不安装 Ruby、不启动 Docker、不新增部署链路。10 篇旧文正文逐字节保持不变。不自动提交或发布。

## Context — 背景与目标

保留现有 Markdown → Git push → GitHub Pages 发布流程，改善个人表达、长文阅读和文章／图片管理。主要写作工具为 Typora，Obsidian 辅助绘图。

已确认的设计方向：同一套布局支持两种配色；浅色为白底、清晰锐利、论文式排版，深色为墨黑／深灰、柔和文字、克制的技术感。首页体现个人风格，文章页优先阅读。

### 当前发现

- `_config.yml` 使用 `pages-themes/minimal@v0.2.0`，站点为 `https://xkai-site.github.io/mylovelyblog`，必须兼容项目子路径。
- `index.html` 已有个人欢迎语、文章标题和日期列表，可复用内容与 Liquid 遍历。
- `_layouts/post.html` 继承远程主题的 `default`，已有标题、日期、正文和返回入口；目前通过大量内联 `!important` 覆盖主题。
- `_includes/head-custom.html` 目前也是主题样式覆盖，无现成的主题切换、目录或内容管理工具。
- 10 篇文章都有 `layout/title/date`，未发现分类、标签或自定义 permalink。
- 现有图片按文章放在 `assets/images/`，Markdown 使用 `/mylovelyblog/assets/images/...` 路径，部分文件名包含中文和空格。旧资源保留，不批量改名。
- 仓库跟踪文件中没有 `.github/workflows/`、Gemfile 或测试配置；不能仅凭本地文件认定远程部署方式，执行前需核对 Pages 构建设置。
- Jekyll 默认文章 URL 可能包含分类。补分类时使用不含分类的固定 URL 规则，避免日常归类导致链接变化；这属于内容稳定性，不要求兼容旧界面。
- 本机 PATH 可找到 Node、Python，未找到 Ruby、Bundler 或 gh；当前不能声称可直接运行 Jekyll。构建环境是执行前置检查项，不在规划阶段安装。
- 已核对 Typora 官方图片文档：https://support.typora.io/Images/ 。支持 `typora-root-url` 和 `typora-copy-images-to`；实际粘贴生成路径仍需在用户的 Typora 中验证。

## Approach — 推荐方案

### 页面与视觉

- 继续使用 Jekyll/Liquid，不换框架、不增加 CMS 或 Node 前端构建链。
- 不保留 Minimal 主题外观或布局约束：移除 `remote_theme` 依赖，用本地 `_layouts/default.html` 与独立样式完整定义界面。仅使用 Jekyll／GitHub Pages 支持的能力，补齐页面标题、描述、canonical 等基础元数据。
- 首页：现有个人介绍、最新文章、分类入口；不强制封面图，摘要可选。
- 文章页：清晰标题层级、受控行宽、桌面侧边目录／移动端折叠目录；无小标题的短文不显示空目录。
- 样式覆盖标题、段落、列表、引用、表格、代码、图片说明、脚注。Obsidian 绘图导出 PNG／SVG 后作为普通图片插入；不加入 MathJax、KaTeX、Mermaid。优先使用 PNG，SVG 仅使用可信的自行导出内容。
- 浅色采用白底、近黑正文、明确的细分隔线；深色采用墨黑／深灰底、柔和浅灰正文与克制蓝青强调色。使用本地系统字体栈，不依赖外部字体服务；避免大片卡片、发光效果与强动画。
- 深浅模式共享布局，首次跟随系统、允许手动切换并记忆；存储不可用时正常降级；正文在禁用 JavaScript 时仍可阅读。
- 分类与归档采用普通静态页面和 Jekyll 自带数据，不依赖 Pages 不支持的自定义插件。标签提供可点击的聚合入口。

### 文章与图片管理

- 元数据约定：`title/date/categories/tags`，`description` 可选；分类先采用少量稳定大类，标签表示具体话题。
- 已获授权整理现有 10 篇文章：阅读全文后拟定“技术／生活／随想”等少量分类及具体标签，列出归类清单供验收，不改标题、日期和正文。无分类文章保留可发现入口。
- 为新文章分配固定资源标识，图片目录不跟随显示标题变化。保留所有旧资源与引用。
- 采用现有 `/mylovelyblog/assets/images/<固定标识>/...` 图片 URL，结合 Typora 的 `typora-root-url`／`typora-copy-images-to` 元数据，使同一份 Markdown 本地与线上均能显示图片。当前仓库目录名与 `baseurl` 匹配，可先验证 `_posts/` 和 `_drafts/` 中 `typora-root-url: ../..`、`typora-copy-images-to: ../assets/images/<固定标识>` 的组合；必须实测粘贴输出及预览，不把候选配置当成已验证结果。文档说明目录重命名／克隆位置变化时的配置条件。
- 不采用正文字符串批量替换，不要求作者手写 Liquid 图片语法，不用浏览器 JavaScript 修复图片路径。闭环若不成立，先报告具体限制并修订路径方案，再继续实现工具。
- 使用本机已有 Node，提供无第三方依赖的 `scripts/new-post.mjs`：生成草稿、元数据、固定图片目录和 Typora 配置；拒绝覆盖同名文件，校验日期／标识／越界路径。Node 仅用于可选写作辅助，不加入站点构建链。
- 草稿放 `_drafts/`，正式发布移至 `_posts/` 并带日期命名。**草稿不出现在网站不等于私密：公开仓库中的草稿和图片仍可被访问；敏感草稿留在仓库外。**
- 写作指南说明新建、粘贴图片、预览、发布、修改标题及分类的操作。

### 不在首版范围

评论系统、账号后台、站内全文搜索、图床迁移、复杂动画、自动改写正文、重构部署流程；不自动 commit 或 push。

## Files to modify — 关键文件

| 路径 | 计划用途 |
| --- | --- |
| `_config.yml` | 移除远程主题、站点信息、布局默认值、不随分类变化的 URL 规则、排除计划／工具／文档 |
| `_layouts/default.html`（新增） | 通用页面骨架、导航、主题初始化、资源与元数据 |
| `_layouts/post.html` | 文章结构、元数据展示、目录区域 |
| `_includes/head-custom.html` | 替换旧主题样式覆盖，作为本地布局的 head 扩展入口 |
| `index.html` | 首页介绍、文章列表、分类入口 |
| `archive.html`、`categories.html`、`tags.html`（新增） | 年份归档、分类与标签聚合 |
| `assets/css/style.scss`（新增） | 双模式变量、排版、响应式和代码高亮样式 |
| `assets/js/theme.js`、`assets/js/post.js`（新增） | 模式切换、目录等渐进增强 |
| `_posts/*.md` | 分类／标签、必要的 Typora 元数据与 URL 固定；不改正文 |
| `templates/post.md`、`docs/writing.md`（新增） | 文章模板与 Typora 写作指南，排除出网站构建 |
| `scripts/new-post.mjs`、`scripts/check-site.mjs`（新增） | 新建文章及构建产物链接／图片检查，无 npm 依赖 |
| `scripts/new-post.test.mjs`（新增） | 使用 Node 内建测试运行器测试工具的边界行为 |
| `.gitignore`（新增） | 忽略 `_site/`、缓存和本地临时文件，不新增本地 Ruby 依赖 |

## Reuse — 可复用内容

- `_config.yml` 的 `url/baseurl`，模板资源链接统一使用 Jekyll 的 `relative_url`。
- `index.html` 的欢迎语、个人简介与 `site.posts` 遍历。
- `_layouts/post.html` 的 `page.title/page.date/content` 渲染逻辑。
- 现有 `_posts/` 文本和 `assets/images/` 图片目录；不迁移旧内容存储。
- Jekyll 内建 categories/tags、日期分组与 Markdown 渲染，不另建索引数据库。

## Steps — 执行步骤

- [x] 1. 确认验证策略：按用户决定复用远端 Actions，本地仅做可运行的静态／工具测试，不安装环境；构建与线上验收交用户。
- [x] 2. 阅读 10 篇旧文并拟定分类／标签；记录文章 URL 与图片基线，定义不随分类变化的 URL 规则。旧界面不作为兼容目标。
- [x] 3. 根据 Typora 文档验证图片路径的文件系统映射，固化模板与规则；实际 Typora 粘贴与远端图片显示列为用户验收项，不声称已做 GUI／构建验证。
- [x] 4. 实现本地通用布局及双模式视觉，兼顾长技术文章和短随笔；实际阅读风格交线上验收。
- [x] 5. 完成文章组件、首页、分类／标签／时间归档与移动端适配。
- [x] 6. 按授权补全旧文元数据，完成新建文章工具及其测试、构建产物检查工具、草稿／图片管理指南；排除工具、模板和文档的站点输出。
- [x] 7. 完成本地可运行的静态／工具测试与源文件检查，提供用户 push 后的构建、链接、交互和视觉验收清单；明确列出未在本地运行的验证。

## Verification — 验证与验收

- 构建：用户 push 后检查现有 Actions 是否成功及线上页面；本次不安装 Ruby，不执行本地 Jekyll 构建。区分日期在未来而按 Jekyll 默认行为未发布的文章，不为展示测试意外开启生产 `future`。
- 内容完整性：10 篇源文章无丢失、正文无非预期改动；应发布文章均可访问；分类调整不改变文章 URL；中文／空格图片路径正确；用 `node scripts/check-site.mjs` 检查构建产物中的内部链接及资源目标，排除外部链接，不发网络请求。
- 工具：`node --test scripts/new-post.test.mjs` 验证草稿模板、标识稳定性、同名拒绝覆盖、非法日期和路径穿越防护；临时测试文件留在隔离临时目录，不改现有文章。
- 写作闭环：新建一篇测试草稿，在 Typora 粘贴图片、本地显示，转为正式文章后网站显示相同图片；改标题不影响图片；分类／标签及归档自动更新。测试内容不进入最终发布。
- 排版：选现有技术文与随笔，加临时排版样例覆盖表格、长代码、脚注、引用等；导出的绘图图片在两种配色下均清晰。对白底图保留中性衬底，不反转图片颜色；公式／Mermaid 源码渲染不在验收范围。
- 模式：浅色／深色、系统偏好、手动覆盖、刷新记忆、存储不可用及无 JavaScript 降级。
- 响应式／无障碍：手机、平板、桌面宽度；200% 缩放；可见键盘焦点、可访问的切换按钮；普通文字对比度至少 4.5:1；长表格／代码不撑破页面。
- 信息管理：无分类、无标签、无摘要、重复或特殊字符标签、无小标题文章均正常；草稿与工具／文档不出现在生产页面。
- 部署：本地验收后由用户 push，核对既有部署任务和线上代表页面；本次不主动改变 CI/CD。

## 交付与验证摘要

- 实现：本地双模式布局、首页／文章／分类／标签／归档、目录／图注／表格增强、10 篇旧文元数据整理、草稿及图片目录生成工具。
- 本地验证：22 项 Node 测试全通过；10 篇文章、12 处图片、8 个模板约定检查通过；10 篇正文 SHA-256 与原始基线完全一致；旧图片未变更。
- 用户指南：`README.md`、`docs/writing.md`、`docs/acceptance.md`；详细本地记录见 `progress.md`。
- 未运行：真实 Jekyll 构建、Typora GUI 粘贴、浏览器布局／交互、远端部署与线上页面测试。上述事项明确交用户验收。

## 执行记录

### 第 1 步：构建环境检查与验证策略调整

- Windows PATH、常见安装目录、软件卸载注册信息均未发现 Ruby/Bundler 安装。
- 已有 Docker CLI 28.3.0，但 Docker Desktop Linux 引擎不可连接（`dockerDesktopLinuxEngine` 管道不存在），无法读取本地镜像清单。尚未启动 Docker 或拉取镜像。
- WSL 已登记 Ubuntu 与 docker-desktop；Ubuntu 的只读工具探测 25 秒超时，未确认其中存在可复用 Ruby 环境；同时输出 WSL localhost 代理提示。
- 公共 GitHub Pages 元数据与 Actions runs API 均返回匿名请求限流，未能确认远程构建设置。仓库内仍未发现工作流文件，不能凭此推断远端配置。
- 暂无可运行的 Jekyll 构建环境，未做构建或 Typora 插图闭环验证。
- 用户已明确同意不准备本地构建环境：由用户 push、触发现有工作流并在线验收。不再以本地环境或 Pages 设置截图阻塞实现，不修改工作流。

## 已确认的边界与执行前置条件

- 用户允许补充旧文分类／标签，保留正文。
- 绘图以导出的图片使用，首版不增加公式／Mermaid 渲染依赖。
- 可以完全重设计界面，只需保持 Jekyll 技术范围；不为旧 Minimal 样式添加兼容层。
- 继续使用现有站点地址、仓库和发布流程；计划审批不等同于授权安装系统环境、修改远程 Pages 设置或 push。
- 按最新确认，Jekyll 构建与 Typora 实际粘贴不再作为实现前置门槛；转交用户实际验收。只报告本地真实完成的检查，不把静态检查当作构建、GUI 或线上验收结果。

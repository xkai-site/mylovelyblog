# 前端重构计划

## 边界与目标
仅调整 Jekyll 前端模板、样式和交互；保留文章内容、内容组织、构建及部署。采用学术期刊／文献目录方向，保留论文式衬线字体与正文排版，完善深浅模式及内容浏览入口。

## 阶段
1. [complete] 检查页面、样式、交互与现有验证方式。
2. [complete] 确定设计细节并实现全站一致的模板与主题。
3. [complete] 验证渲染、主题切换、响应式与浏览交互，整理交付。

## 错误记录
- 环境探测命令末尾 Ruby 目录不存在，返回 2；确认无 Ruby／Jekyll。使用临时前端预览及语法／逻辑验证，不改构建配置，也不把替代预览当作真实 Jekyll 构建。

- 最终浏览器断言使用跨运行域数组的 deepEqual 导致断言库报错（实际 axe 返回空数组）；改用数值 length 断言，并只读复查当前页面，不重复交互。

## 实施决策
- 纸白／暖黑 + 朱红，保留现有 Georgia／宋体栈并补充 Windows 宋体回退，不加载远程字体。
- 首页文集刊头 + 主文章目录／侧栏索引；分类与标签页使用目录式双栏。
- 三态文字主题按钮，继续支持系统偏好、存储失败与跨标签页同步。
- 归档检索标题、分类、标签与日期；无 JavaScript 时保留完整内容。
- 计划日志交付时归入已排除部署的 docs/frontend-redesign/，避免进入站点。

## 本轮减法调整
1. [complete] 确认冗余来源：组合 Logo、首页重复站名与介绍、英文栏目名、编号、重复计数、页面使用说明、文末固定口号。
2. [complete] 改为单行文字站名，正文优先；移除装饰与重复说明，保留导航、无障碍标签及必要的搜索反馈。
3. [complete] 编译临时预览并验证响应式、主题、检索、锚点和页面截图。Sass、LiquidJS 渲染及 JS 语法检查通过；已查看桌面首页，单行站名后直接进入近作，无重复刊头与装饰文案。14 页面 × 5 视口、站内锚点、搜索与主题回归通过；5 个代表页深浅主题 axe 无违规。单一年份不再重复显示导航；最终手机截图、搜索清除及 axe 复查通过。

## 交付状态
前端实现及本地浏览器验证完成；验证详情见 verification.md。真实 Jekyll／GitHub Actions 构建留待既有发布流程验收；未提交或推送。
